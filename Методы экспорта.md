def _export_generated_passwords(self):
    print("\n📋 Генерация паролей для экспорта:")
    count = int(input("Сколько паролей сгенерировать? (1-50): "))
    count = max(1, min(50, count))  # Ограничение 1-50
    length = int(input("Длина паролей (8-32): "))
    length = max(8, min(32, length))  # Ограничение 8-32
    print("Тип паролей:")
    print("1 - Смешанные (буквы, цифры, символы)")
    print("2 - Только буквы")
    print("3 - Буквы и цифры")
    type_choice = input("Ваш выбор (1-3): ")
    passwords = []
    for i in range(count):
        if type_choice == "1":
            password = self.generate_advanced_password(length)  # Все символы
        elif type_choice == "2":
            chars = self.lowercase + self.uppercase  # Только буквы
            password = ''.join(random.choice(chars) for _ in range(length))
        else:
            chars = self.lowercase + self.uppercase + self.digits  # Буквы+цифры
            password = ''.join(random.choice(chars) for _ in range(length))
        passwords.append(password)
    filename = f"passwords_export
