def mode12_memory_game(self):
    self._update_stats("mode12")
    print("\n" + "=" * 40)
    print("🎮 РЕЖИМ 12: Тренировка памяти паролей")
    print("=" * 40)
    print("\nВыберите уровень сложности:")
    print("1 - Легкий (6 символов)")
    print("2 - Средний (8 символов)")
    print("3 - Сложный (10 символов)")
    print("4 - Эксперт (12 символов)")
    level_choice = input("Ваш выбор (1-4): ")
    levels = {"1": 6, "2": 8, "3": 10, "4": 12}
    length = levels.get(level_choice, 8)
    original = self.generate_advanced_password(length)
    print(f"\n🧠 ЗАПОМНИТЕ ПАРОЛЬ:")
    print(f"🔐 {original}")
    input("\n⏎ Нажмите Enter, когда запомните...")
    print("\n" * 50)
    print("=" * 40)
    print("ВРЕМЯ ПРОВЕРКИ ПАМЯТИ!")
    print("=" * 40)
    for attempt in range(3):
        guess = input(f"\nПопытка {attempt + 1}/3. Введите пароль: ")
        if guess == original:
            print(f"\n🎉 Поздравляем! Вы правильно запомнили пароль!")
            print(f"🏆 Уровень: {'Легкий' if length == 6 else 'Средний' if length == 8 else 'Сложный' if length == 10 else 'Эксперт'}")
            print(f"⏱️  Попыток использовано: {attempt + 1}")
            if "games_played" not in self.stats:
                self.stats["games_played"] = 0
                self.stats["games_won"] = 0
            self.stats["games_played"] += 1
            self.stats["games_won"] += 1
            self.save_stats()
            return True
        else:
            correct_chars = sum(1 for g, o in zip(guess, original) if g == o)
            print(f"❌ Неверно. Совпадающих символов: {correct_chars}/{length}")
            if attempt < 2:
                print(f"💡 Подсказка: первые {min(3, length)} символа: {original[:3]}")
    print(f"\n💔 Игра окончена. Пароль был: {original}")
    if "games_played" not in self.stats:
        self.stats["games_played"] = 0
        self.stats["games_won"] = 0
    self.stats["games_played"] += 1
    self.save_stats()

    return False
