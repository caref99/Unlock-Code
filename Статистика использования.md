def mode9_statistics(self):
    self._update_stats("mode9")
    print("\n" + "=" * 40)
    print("📈 РЕЖИМ 9: Статистика использования")
    print("=" * 40)
    self._check_reset_daily_stats()  # Сброс дневной статистики
    print("\n📊 ОБЩАЯ СТАТИСТИКА:")
    print(f"👤 Всего сгенерировано паролей: {self.stats.get('generated', 0)}")
    print(f"📅 Сегодня сгенерировано: {self.stats.get('generated_today', 0)}")
    print(f"💾 Сохранено паролей: {len(self.password_manager.list_services())}")
    if self.stats.get('mode_usage'):
        print("\n🎯 СТАТИСТИКА ПО РЕЖИМАМ:")
        for mode, count in sorted(self.stats['mode_usage'].items(), 
                                  key=lambda x: x[1], reverse=True):  # Сортировка по убыванию
            mode_name = self._get_mode_name(mode)
            print(f"  {mode_name}: {count} раз")
    if self.stats.get('mode_usage'):
        most_used = max(self.stats['mode_usage'].items(), key=lambda x: x[1])  # Максимальное значение
        mode_name = self._get_mode_name(most_used[0])
        print(f"\n🏆 Самый популярный режим: {mode_name} ({most_used[1]} раз)")
    print(f"\n📅 Статистика обновлена: {self.stats.get('last_reset', 'Нет данных')}")
