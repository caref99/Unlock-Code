def mode8_password_transformer(self):
    self._update_stats("mode8")
    print("\n" + "=" * 40)
    print("🔄 РЕЖИМ 8: Преобразование паролей")
    print("=" * 40)
    password = input("Введите пароль для преобразования: ")
    if not password:
        print("❌ Пароль не может быть пустым!")
        return
    print("\nВыберите тип преобразования:")
    print("1 - Leet speak (a→@, e→3, i→1)")
    print("2 - Чередование регистра (HeLlO)")
    print("3 - Обратный порядок")
    print("4 - Добавить суффикс")
    print("5 - Преобразовать в верхний регистр")
    print("6 - Преобразовать в нижний регистр")
    choice = input("Ваш выбор (1-6): ")
    transformations = {
        "1": ("leet", lambda s: s.replace('e', '3').replace('E', '3')
              .replace('a', '@').replace('A', '@')
              .replace('i', '1').replace('I', '1')
              .replace('o', '0').replace('O', '0')
              .replace('s', '$').replace('S', '$')),
        "2": ("alternating_case", lambda s: ''.join(
            c.upper() if i % 2 == 0 else c.lower() for i, c in enumerate(s))),
        "3": ("reverse", lambda s: s[::-1]),
        "4": ("add_suffix", lambda s: s + str(random.randint(10, 99)) + "!"),
        "5": ("uppercase", lambda s: s.upper()),
        "6": ("lowercase", lambda s: s.lower())
    }
    if choice in transformations:
        name, transform_func = transformations[choice]
        result = transform_func(password)
        print(f"\n🔄 Преобразование: {name}")
        print(f"📥 Исходный: {password}")
        print(f"📤 Результат: {result}")
        print(f"📊 Сложность: {self.password_strength(result)}")
    else:
        print("❌ Неверный выбор!")
